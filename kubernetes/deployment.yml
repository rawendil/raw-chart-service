apiVersion: apps/v1
kind: Deployment
metadata:
  name: rawchart-service
  labels:
    app: rawchart-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: rawchart-service
  template:
    metadata:
      labels:
        app: rawchart-service
    spec:
      containers:
      - name: rawchart-service
        image: rawchart-service:latest
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: rawchart-service-secrets
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            secretKeyRef:
              name: rawchart-service-secrets
              key: DB_PORT
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: rawchart-service-secrets
              key: DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: rawchart-service-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rawchart-service-secrets
              key: DB_PASSWORD
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: rawchart-service-secrets
              key: API_KEY
        - name: ALLOWED_ORIGINS
          value: "https://yourdomain.com"
        - name: LOG_LEVEL
          value: "info"
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: rawchart-service-config
              key: REDIS_URL
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health/detailed
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: rawchart-service
  labels:
    app: rawchart-service
spec:
  selector:
    app: rawchart-service
  ports:
  - name: http
    port: 80
    targetPort: 3000
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rawchart-service-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - api.yourdomain.com
    secretName: rawchart-service-tls
  rules:
  - host: api.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rawchart-service
            port:
              number: 80

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: rawchart-service-config
data:
  REDIS_URL: "redis://redis-service:6379"

---
apiVersion: v1
kind: Secret
metadata:
  name: rawchart-service-secrets
type: Opaque
data:
  # Base64 encoded values - update these with your actual values
  DB_HOST: "your-database-host"
  DB_PORT: "5432"
  DB_NAME: "rawchart_service"
  DB_USER: "your-db-user"
  DB_PASSWORD: "your-db-password"
  API_KEY: "change-me-in-production-min-16-chars"